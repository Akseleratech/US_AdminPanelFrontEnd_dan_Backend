rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // OPEN RULES FOR DEVELOPMENT - ALLOWS ALL OPERATIONS
    match /{document=**} {
      allow read, write: if true;
    }
  }
}